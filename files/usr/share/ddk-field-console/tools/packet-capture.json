{
  "id": "packet-capture",
  "name": "Capture & Traffic",
  "category": "Packet Capture",
  "description": "Packet capture, rotating files, inspection, and replay from reusable captures.",
  "package_names": ["tcpdump", "tcpreplay", "netsniff-ng", "dnstop", "nfdump", "softflowd", "hping3"],
  "expected_binaries": ["tcpdump", "tcpreplay", "netsniff-ng", "dnstop"],
  "required_hardware": [],
  "status_probe": "software",
  "risk_level": "SECURITY",
  "enabled": true,
  "actions": [
    {"id": "capture.lan_metadata_snapshot", "class": "SECURITY", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "capture.replay", "class": "DISRUPTIVE", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "capture.inspect", "label": "Inspect a saved capture", "class": "INFO", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "capture.ring", "label": "Rotating packet capture", "class": "SECURITY", "execution": "job", "parameter_schema": "operator-v1", "enabled": true}
  ],
  "help_text": "Use rotating capture for long observations, inspect a saved PCAP, or reuse it in the replay workflow. Stop keeps partial captures. Output budgets are checked against available storage."
}
