{
  "id": "packet-capture",
  "name": "Capture & Traffic",
  "category": "Packet Capture",
  "description": "Native tcpdump 4.9.3 capture with validated live interfaces, BPF filters, decode controls, and authenticated PCAP artifacts.",
  "package_names": ["tcpdump", "tcpreplay", "netsniff-ng", "dnstop", "nfdump", "softflowd", "hping3"],
  "expected_binaries": ["tcpdump", "tcpreplay", "netsniff-ng", "dnstop"],
  "required_hardware": [],
  "status_probe": "software",
  "risk_level": "SECURITY",
  "enabled": true,
  "actions": [
    {"id": "capture.lan_metadata_snapshot", "class": "SECURITY", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "capture.replay", "class": "DISRUPTIVE", "execution": "job", "parameter_schema": "operator-v1", "enabled": true}
  ],
  "help_text": "Capture supports structured BPF/decode/PCAP workflows. Replay separately binds a sealed validated PCAP to an exact current interface with loop, packet, duration, and native speed controls plus target-bound confirmation and cancellation."
}
