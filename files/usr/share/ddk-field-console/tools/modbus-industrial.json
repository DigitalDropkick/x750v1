{
  "id": "modbus-industrial",
  "name": "Industrial / Modbus",
  "category": "Industrial",
  "description": "Modbus TCP and RTU reads, writes, polling, and write verification.",
  "package_names": ["mbtools", "mbusd", "libmodbus", "linuxptp", "ntripcaster", "ntripclient", "ntripserver"],
  "expected_binaries": ["mbpoll", "mbusd", "ptp4l", "ntripclient"],
  "required_hardware": [],
  "status_probe": "software",
  "risk_level": "ACTION",
  "enabled": true,
  "actions": [
    {"id": "industrial.modbus_read", "class": "ACTION", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "industrial.modbus_write", "label": "Modbus reads, polling, and writes", "class": "DISRUPTIVE", "execution": "job", "parameter_schema": "operator-v1", "enabled": true}
  ],
  "help_text": "The native libmodbus adapter supports coils, discrete inputs, holding and input registers, single or multiple writes, polling, and readback verification. Choose the transport, unit, address and values explicitly."
}
