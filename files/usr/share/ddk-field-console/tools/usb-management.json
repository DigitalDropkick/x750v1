{
  "id": "usb-management",
  "name": "USB Hardware",
  "category": "USB & Serial",
  "description": "USB inventory, topology-aware hub power, and USB/IP import management.",
  "package_names": ["usbutils", "uhubctl", "usbip", "usbip-client", "usbip-server"],
  "expected_binaries": ["lsusb", "uhubctl", "usbip"],
  "required_hardware": ["usb"],
  "status_probe": "usb",
  "risk_level": "DISRUPTIVE",
  "enabled": true,
  "actions": [
    {"id": "usb.inventory", "class": "INFO", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "usb.power", "label": "USB hub port power", "class": "DISRUPTIVE", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "usbip.attach", "label": "USB/IP connections", "class": "DISRUPTIVE", "execution": "job", "parameter_schema": "operator-v1", "enabled": true}
  ],
  "help_text": "Hub power controls identify the selected port and its downstream devices. Active router storage, swap and uplink devices are protected. USB/IP lists remote devices, imports them into real VHCI ports, and detaches selected imports."
}
