{
  "id": "serial",
  "name": "USB & Serial Attribution",
  "category": "Serial",
  "description": "Interactive serial console, receive logging, and binary transfers to selected USB adapters.",
  "package_names": ["picocom", "socat", "ser2net", "conserver", "gensio-bin"],
  "expected_binaries": ["picocom", "socat", "ser2net", "conserver"],
  "required_hardware": ["serial"],
  "status_probe": "serial",
  "risk_level": "ACTION",
  "enabled": true,
  "actions": [
    {"id": "serial.inspect", "class": "INFO", "enabled": true},
    {"id": "serial.session", "class": "ACTION", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "serial.console", "label": "Interactive serial console", "class": "ACTION", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "serial.transfer", "label": "Serial file transfer", "class": "ACTION", "execution": "job", "parameter_schema": "operator-v1", "enabled": true}
  ],
  "help_text": "Choose the adapter and line settings, start a console, and send text or hexadecimal data from Jobs. File transfer sends sealed input and records received bytes. Modem AT commands have a dedicated cellular workflow."
}
