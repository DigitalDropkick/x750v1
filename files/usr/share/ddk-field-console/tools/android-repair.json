{
  "id": "android-repair",
  "name": "Android / ADB",
  "category": "Device Repair",
  "description": "Android diagnostics, files, applications, logs, backups, and device controls over USB or network ADB.",
  "package_names": ["adb"],
  "expected_binaries": ["adb"],
  "required_hardware": ["android"],
  "status_probe": "android",
  "risk_level": "ACTION",
  "enabled": true,
  "no_device_state": "READY / NO DEVICE",
  "actions": [
    {"id": "android.identify", "class": "INFO", "execution": "info", "enabled": true},
    {"id": "android.operator_guide", "class": "INFO", "execution": "info", "enabled": true},
    {"id": "android.adb_diagnostics", "class": "ACTION", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "android.adb_manage", "class": "DISRUPTIVE", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "android.operator", "label": "Android device tools", "class": "DISRUPTIVE", "execution": "job", "parameter_schema": "operator-v1", "enabled": true}
  ],
  "help_text": "Use Android Tools for USB or TCP targets, file and directory transfers, single or split APK installation, logs, services, and device controls. Authorize debugging on the device. The router uses its own temporary ADB server; supported device operations depend on Android and the installed ADB build."
}
