{
  "id": "camera",
  "name": "Camera / Video",
  "category": "Camera",
  "description": "Structured UVC still capture and temporary authenticated local-address streaming.",
  "package_names": ["fswebcam", "mjpg-streamer", "motion", "v4l-utils", "v4l2rtspserver", "v4l2tools"],
  "expected_binaries": ["fswebcam", "mjpg_streamer", "motion", "v4l2-ctl"],
  "required_hardware": ["video"],
  "status_probe": "video",
  "risk_level": "ACTION",
  "enabled": true,
  "actions": [
    {"id": "camera.still_snapshot", "class": "ACTION", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "camera.stream", "class": "DISRUPTIVE", "execution": "job", "parameter_schema": "operator-v1", "enabled": true}
  ],
  "help_text": "Still capture exposes native image controls and artifacts. Streaming is an on-demand mjpg-streamer job bound only to an exact current local address, requires private Basic credentials and target confirmation, disables plugin commands, and stops on duration/cancellation without enabling the installed service."
}
