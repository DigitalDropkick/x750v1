{
  "id": "camera",
  "name": "Camera / Video",
  "category": "Camera",
  "description": "One bounded local UVC still capture plus disabled streaming and motion placeholders.",
  "package_names": ["fswebcam", "mjpg-streamer", "motion", "v4l-utils", "v4l2rtspserver", "v4l2tools"],
  "expected_binaries": ["fswebcam", "mjpg_streamer", "motion", "v4l2-ctl"],
  "required_hardware": ["video"],
  "status_probe": "video",
  "risk_level": "ACTION",
  "enabled": true,
  "actions": [
    {"id": "camera.still_snapshot", "class": "ACTION", "execution": "job", "enabled": true},
    {"id": "camera.stream", "class": "DISRUPTIVE", "execution": "job", "enabled": false}
  ],
  "help_text": "Still capture requires exactly one sysfs-attributed USB UVC camera and one primary capture node. Streaming, Motion, RTSP, audio, and uploads stay disabled."
}
