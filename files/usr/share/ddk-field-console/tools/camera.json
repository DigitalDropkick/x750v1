{
  "id": "camera",
  "name": "Camera / Video",
  "category": "Camera",
  "description": "Camera snapshots, controls, recording, and authenticated live viewing.",
  "package_names": ["fswebcam", "mjpg-streamer", "motion", "v4l-utils", "v4l2rtspserver", "v4l2tools"],
  "expected_binaries": ["fswebcam", "mjpg_streamer", "motion", "v4l2-ctl"],
  "required_hardware": ["video"],
  "status_probe": "video",
  "risk_level": "ACTION",
  "enabled": true,
  "actions": [
    {"id": "camera.still_snapshot", "class": "ACTION", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "camera.stream", "class": "DISRUPTIVE", "execution": "job", "parameter_schema": "operator-v1", "enabled": true},
    {"id": "camera.video", "label": "Camera recording and controls", "class": "ACTION", "execution": "job", "parameter_schema": "operator-v1", "enabled": true}
  ],
  "help_text": "Choose a camera, inspect supported formats/controls, take a still, record frames, or open a temporary authenticated stream. Ownership is checked for the selected device. Stop retains partial recordings."
}
